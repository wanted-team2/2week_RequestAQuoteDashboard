import { Button } from '@components/base';

const Buttons = () => {
  return (
    <>
      <Button blue={true}>요청 내역 보기</Button>
      <Button blue={false}>채팅하기</Button>
    </>
  );
};

export default Buttons;
